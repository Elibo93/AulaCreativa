# Aplicación
spring.application.name=aulaCreativa
api.version=1.0.0
server.error.include-message=always
server.port=8443

# # Perfil
# spring.profiles.active=prod

# --- BACKEND (MySQL) --- # En producción se configura en application-prod.properties
# Conexión a la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/artia_db
spring.datasource.username=root
spring.datasource.password=root

spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.defer-datasource-initialization=true

spring.sql.init.mode=always

# --- FRONTEND / VIEWS (Thymeleaf) ---
# Thymeleaf (desactivar cache en desarrollo)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

# Mensajes / i18n
spring.messages.encoding=UTF-8
spring.messages.basename=messages

# Logging general (dev)
# logging.level.org.springframework=DEBUG
# logging.level.org.springframework.web=DEBUG


server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=mi_contrasena_segura
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=springboot